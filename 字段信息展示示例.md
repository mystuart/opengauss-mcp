# 字段信息展示示例

## 新旧输出格式对比

### 原始格式（优化前）

```json
[
  {
    "column": "id",
    "data_type": "integer",
    "is_nullable": "NO",
    "default": "nextval('users_id_seq'::regclass)"
  },
  {
    "column": "name",
    "data_type": "character varying",
    "is_nullable": "NO",
    "default": null
  },
  {
    "column": "email",
    "data_type": "character varying",
    "is_nullable": "YES",
    "default": null
  },
  {
    "column": "age",
    "data_type": "integer",
    "is_nullable": "YES",
    "default": null
  },
  {
    "column": "salary",
    "data_type": "numeric",
    "is_nullable": "YES",
    "default": null
  },
  {
    "column": "created_at",
    "data_type": "timestamp without time zone",
    "is_nullable": "NO",
    "default": "now()"
  },
  {
    "column": "profile",
    "data_type": "text",
    "is_nullable": "YES",
    "default": null
  },
  {
    "column": "is_active",
    "data_type": "boolean",
    "is_nullable": "YES",
    "default": "true"
  },
  {
    "column": "user_uuid",
    "data_type": "uuid",
    "is_nullable": "NO",
    "default": "gen_random_uuid()"
  },
  {
    "column": "settings",
    "data_type": "jsonb",
    "is_nullable": "YES",
    "default": null
  }
]
```

### 增强格式（优化后）

```json
[
  {
    "column": "id",
    "data_type": "integer",
    "is_nullable": "NO",
    "default": "nextval('users_id_seq'::regclass)",
    "type_details": {
      "size": "4 bytes"
    }
  },
  {
    "column": "name",
    "data_type": "character varying",
    "is_nullable": "NO",
    "default": null,
    "type_details": {
      "length": "255 characters",
      "size": "255 bytes"
    }
  },
  {
    "column": "email",
    "data_type": "character varying",
    "is_nullable": "YES",
    "default": null,
    "type_details": {
      "length": "320 characters",
      "size": "320 bytes"
    }
  },
  {
    "column": "age",
    "data_type": "integer",
    "is_nullable": "YES",
    "default": null,
    "type_details": {
      "size": "4 bytes"
    }
  },
  {
    "column": "salary",
    "data_type": "numeric",
    "is_nullable": "YES",
    "default": null,
    "type_details": {
      "precision": "(10, 2)"
    }
  },
  {
    "column": "created_at",
    "data_type": "timestamp without time zone",
    "is_nullable": "NO",
    "default": "now()",
    "type_details": {
      "size": "8 bytes"
    }
  },
  {
    "column": "profile",
    "data_type": "text",
    "is_nullable": "YES",
    "default": null,
    "type_details": {
      "size": "variable"
    }
  },
  {
    "column": "is_active",
    "data_type": "boolean",
    "is_nullable": "YES",
    "default": "true",
    "type_details": {
      "size": "1 byte"
    }
  },
  {
    "column": "user_uuid",
    "data_type": "uuid",
    "is_nullable": "NO",
    "default": "gen_random_uuid()",
    "type_details": {
      "size": "16 bytes"
    }
  },
  {
    "column": "settings",
    "data_type": "jsonb",
    "is_nullable": "YES",
    "default": null,
    "type_details": {
      "size": "variable + overhead"
    }
  }
]
```

## 详细信息解读

### 1. 整数类型字段 (integer)
```json
{
  "column": "id",
  "type_details": {
    "size": "4 bytes"
  }
}
```
**解读**: 占用 4 字节存储空间，范围约 -2.1亿 到 2.1亿

### 2. 变长字符串类型 (varchar)
```json
{
  "column": "email",
  "type_details": {
    "length": "320 characters",  // 最长 320 个字符
    "size": "320 bytes"          // 占用 320 字节
  }
}
```
**解读**: 支持最多 320 个字符的邮箱地址，UTF-8 编码下每个字符占用 1 字节

### 3. 数值类型 (numeric)
```json
{
  "column": "salary",
  "type_details": {
    "precision": "(10, 2)"  // 总共 10 位数字，其中 2 位小数
  }
}
```
**解读**: 可以存储最大 99999999.99 的数值

### 4. 时间戳类型 (timestamp)
```json
{
  "column": "created_at",
  "type_details": {
    "size": "8 bytes"
  }
}
```
**解读**: 占用固定 8 字节存储，精度到微秒

### 5. 文本类型 (text)
```json
{
  "column": "profile",
  "type_details": {
    "size": "variable"
  }
}
```
**解读**: 变长存储，根据实际内容占用空间

### 6. 布尔类型 (boolean)
```json
{
  "column": "is_active",
  "type_details": {
    "size": "1 byte"
  }
}
```
**解读**: 只占用 1 字节，存储 true/false

### 7. UUID 类型
```json
{
  "column": "user_uuid",
  "type_details": {
    "size": "16 bytes"
  }
}
```
**解读**: 固定 16 字节，存储唯一标识符

### 8. JSONB 类型
```json
{
  "column": "settings",
  "type_details": {
    "size": "variable + overhead"
  }
}
```
**解读**: 变长存储，带有额外的元数据开销

## 使用场景示例

### 场景 1: 存储空间分析
通过查看 `type_details.size`，可以快速估算表的存储需求：

```
表 users 总存储估算:
- id (integer): 4 bytes × 10000 行 = 40 KB
- name (varchar(255)): 255 bytes × 10000 行 = 2.55 MB
- email (varchar(320)): 320 bytes × 10000 行 = 3.2 MB
- created_at (timestamp): 8 bytes × 10000 行 = 80 KB
- ... (其他字段)
```

### 场景 2: 数据类型优化建议
通过分析字段使用情况：

```
建议优化:
1. age 字段从 integer 改为 smallint (节省 2 字节/行)
2. name 字段从 varchar(255) 改为 varchar(100) (节省 155 字节/行)
3. created_at 可以考虑 date 类型 (如果不需要时间部分)
```

### 场景 3: 字段类型匹配检查
在数据迁移时，对比源表和目标表：

```
源表字段: email (varchar(320))
目标表字段: email (varchar(255))
⚠️ 警告: 目标表字段长度不足！
```

## 总结

新的字段信息格式提供了丰富的元数据：
- ✅ 存储空间信息
- ✅ 精度和长度信息
- ✅ 字符集和编码信息
- ✅ 便于性能优化
- ✅ 便于数据迁移
- ✅ 便于存储规划

这使得 `get_object_details` 工具不仅能展示字段定义，还能提供实用的性能优化建议！
